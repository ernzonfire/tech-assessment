const entry = (category, short, meaning, pattern, example) => ({
  category,
  short,
  meaning,
  pattern,
  example,
});

export const FUNDAMENTAL_REFERENCE = {
  "console.log": entry(
    "Debugging output",
    "Prints a value to the developer console.",
    "Use console.log to inspect values while debugging. It does not display content in the page and it does not return the logged value.",
    "console.log(value1, value2, ...)",
    "const total = 120;\nconsole.log(\"total:\", total); // visible in DevTools console",
  ),
  "console.error": entry(
    "Debugging output",
    "Prints an error message to the developer console.",
    "Use it for diagnostic information. A real user still needs a visible, understandable error state in the interface.",
    "console.error(message, error?)",
    "try {\n  await save();\n} catch (error) {\n  console.error(\"Save failed\", error);\n}",
  ),
  "console.table": entry(
    "Debugging output",
    "Displays arrays or objects as a table in the developer console.",
    "It is useful for scanning repeated object properties during debugging, but it is not user-facing output.",
    "console.table(arrayOrObject)",
    "console.table([{ name: \"Ana\", score: 90 }, { name: \"Ben\", score: 80 }]);",
  ),
  "alert": entry(
    "Browser output",
    "Shows a blocking browser message dialog.",
    "It can demonstrate output in tiny exercises, but real interfaces usually render messages in the page because alert blocks interaction and is hard to style.",
    "alert(message)",
    "alert(\"Saved\");",
  ),
  "textContent": entry(
    "DOM property",
    "Reads or replaces the text inside an element.",
    "This is a safe default for displaying user-provided text because the browser treats the value as text, not HTML.",
    "element.textContent = textValue",
    "const output = document.querySelector(\"#output\");\noutput.textContent = \"Hello, Ana!\";",
  ),
  "innerHTML": entry(
    "DOM property",
    "Reads or replaces an element's HTML markup.",
    "Do not insert untrusted text with innerHTML because it can create cross-site scripting vulnerabilities. Prefer textContent or safe DOM construction.",
    "element.innerHTML = trustedHtml",
    "// Prefer this for user input:\nelement.textContent = userInput;",
  ),
  ".trim": entry(
    "String method",
    "Removes whitespace from both ends of a string.",
    "It returns a new string and does not change the original. Spaces inside the text stay unchanged.",
    "string.trim()",
    "const raw = \"  Ana  \";\nconst clean = raw.trim(); // \"Ana\"",
  ),
  ".toUpperCase": entry(
    "String method",
    "Returns an uppercase copy of a string.",
    "The original string does not change because strings are immutable.",
    "string.toUpperCase()",
    "const code = \"php\".toUpperCase(); // \"PHP\"",
  ),
  ".toLowerCase": entry(
    "String method",
    "Returns a lowercase copy of a string.",
    "It is commonly used to normalize text before case-insensitive comparison or search.",
    "string.toLowerCase()",
    "const matches = title.toLowerCase().includes(search.toLowerCase());",
  ),
  ".includes": entry(
    "String/Array method",
    "Checks whether a string or array contains a value.",
    "It returns a boolean. String matching is case-sensitive unless you normalize the text first.",
    "value.includes(searchValue)",
    "\"JavaScript\".includes(\"Script\"); // true\n[1, 2, 3].includes(2);       // true",
  ),
  ".startsWith": entry(
    "String method",
    "Checks whether a string begins with specified text.",
    "It returns true or false and is case-sensitive.",
    "string.startsWith(searchText)",
    "\"javascript\".startsWith(\"java\"); // true",
  ),
  ".endsWith": entry(
    "String method",
    "Checks whether a string ends with specified text.",
    "It is useful for simple suffix checks, but file/type validation must not rely on a filename alone.",
    "string.endsWith(searchText)",
    "\"photo.png\".endsWith(\".png\"); // true",
  ),
  ".slice": entry(
    "String/Array method",
    "Returns a selected section without changing the original value.",
    "The start index is included and the end index is excluded. Negative indexes count from the end.",
    "value.slice(startIndex, endIndex?)",
    "\"JavaScript\".slice(0, 4); // \"Java\"\n[1, 2, 3].slice(1);       // [2, 3]",
  ),
  ".split": entry(
    "String method",
    "Splits a string into an array using a separator.",
    "The separator may be a string or regular expression. Splitting an empty string has edge cases, so validate when necessary.",
    "string.split(separator)",
    "\"red,green,blue\".split(\",\"); // [\"red\", \"green\", \"blue\"]",
  ),
  ".replace": entry(
    "String method",
    "Returns a string with a matched section replaced.",
    "With a string search value it replaces the first match. Use replaceAll or a global regular expression for every match.",
    "string.replace(searchValue, replacement)",
    "\"hello world\".replace(\"world\", \"MERN\"); // \"hello MERN\"",
  ),
  ".length": entry(
    "String/Array property",
    "Reports the number of characters in a string or items in an array.",
    "It is a property, so do not add parentheses. String length counts UTF-16 code units, which matters for some emoji.",
    "value.length",
    "\"hello\".length;   // 5\n[10, 20].length; // 2",
  ),
  "String": entry(
    "Conversion function",
    "Converts a value into a string.",
    "Explicit conversion is clearer than depending on automatic coercion.",
    "String(value)",
    "String(42);       // \"42\"\nString(false);    // \"false\"",
  ),
  "Number": entry(
    "Conversion function",
    "Converts a value into a number.",
    "Invalid numeric text becomes NaN. A blank string becomes 0, so validate text before conversion when blank should be rejected.",
    "Number(value)",
    "Number(\"42\");  // 42\nNumber(\"no\");  // NaN\nNumber(\"\");    // 0",
  ),
  "Boolean": entry(
    "Conversion function",
    "Converts a value according to JavaScript truthy/falsy rules.",
    "The string \"false\" is truthy because it is non-empty. Boolean does not parse human words.",
    "Boolean(value)",
    "Boolean(1);       // true\nBoolean(0);       // false\nBoolean(\"false\"); // true",
  ),
  "parseInt": entry(
    "Number function",
    "Parses an integer from the beginning of text.",
    "Pass radix 10 for decimal input. It can accept trailing non-numeric text, so Number is stricter when the entire value must be numeric.",
    "parseInt(text, 10)",
    "parseInt(\"42px\", 10); // 42",
  ),
  "parseFloat": entry(
    "Number function",
    "Parses a decimal number from the beginning of text.",
    "Like parseInt, it may ignore trailing invalid text.",
    "parseFloat(text)",
    "parseFloat(\"19.95kg\"); // 19.95",
  ),
  "Number.isNaN": entry(
    "Number method",
    "Checks specifically whether a value is the number NaN.",
    "Prefer it over the global isNaN function because it does not coerce other values first.",
    "Number.isNaN(value)",
    "Number.isNaN(NaN);  // true\nNumber.isNaN(\"x\");  // false",
  ),
  "Math.round": entry(
    "Math method",
    "Rounds to the nearest integer.",
    "Half values generally round toward positive infinity.",
    "Math.round(number)",
    "Math.round(4.4); // 4\nMath.round(4.5); // 5",
  ),
  "Math.floor": entry(
    "Math method",
    "Rounds down toward negative infinity.",
    "For negative values, down means more negative—not toward zero.",
    "Math.floor(number)",
    "Math.floor(4.9);  // 4\nMath.floor(-1.2); // -2",
  ),
  "Math.ceil": entry(
    "Math method",
    "Rounds up toward positive infinity.",
    "It is commonly used to calculate how many pages or batches are required.",
    "Math.ceil(number)",
    "Math.ceil(21 / 10); // 3 pages",
  ),
  "Math.max": entry(
    "Math method",
    "Returns the largest of the provided numbers.",
    "It accepts separate arguments, not an array directly. Spreading a very large array can be unsafe; use a loop/reduce instead.",
    "Math.max(number1, number2, ...)",
    "Math.max(3, 9, 4);       // 9\nMath.max(...[3, 9, 4]);  // 9",
  ),
  "Math.min": entry(
    "Math method",
    "Returns the smallest of the provided numbers.",
    "As with Math.max, array values must be spread or processed with a loop.",
    "Math.min(number1, number2, ...)",
    "Math.min(3, 9, 4); // 3",
  ),
  "Math.random": entry(
    "Math method",
    "Returns a pseudo-random number from 0 inclusive to 1 exclusive.",
    "It is fine for games/examples but not for passwords, security tokens, or cryptographic randomness.",
    "Math.random()",
    "const die = Math.floor(Math.random() * 6) + 1;",
  ),
  "%": entry(
    "Remainder operator",
    "Returns the remainder after division.",
    "It is commonly used for even/odd checks and repeating cycles. JavaScript's % is a remainder operator, so negative results can stay negative.",
    "leftNumber % rightNumber",
    "10 % 3; // 1\n8 % 2;  // 0, so 8 is even",
  ),
  "&&": entry(
    "Logical AND operator",
    "Returns the first falsy operand, or the last operand when all are truthy.",
    "In conditions it means both sides must be truthy. It returns operand values, not always a boolean.",
    "leftCondition && rightCondition",
    "const valid = age >= 18 && hasId;",
  ),
  "||": entry(
    "Logical OR operator",
    "Returns the first truthy operand, or the last operand when all are falsy.",
    "It treats 0 and an empty string as missing. Use ?? when those are valid values.",
    "value || fallback",
    "const displayName = user.name || \"Guest\";",
  ),
  "!": entry(
    "Logical NOT operator",
    "Converts a value to boolean and reverses it.",
    "It is useful for toggles and negative conditions. Double NOT (!!) converts a value to boolean.",
    "!value",
    "const nextActive = !user.active;",
  ),
  "!==": entry(
    "Comparison operator",
    "Checks strict inequality without type conversion.",
    "It is the opposite of strict equality (===).",
    "leftValue !== rightValue",
    "5 !== \"5\"; // true because the types differ",
  ),
  "<=": entry(
    "Comparison operator",
    "Checks whether the left value is less than or equal to the right value.",
    "It returns a boolean and is useful for upper bounds.",
    "leftValue <= rightValue",
    "quantity <= stock;",
  ),
  "switch": entry(
    "JavaScript keyword",
    "Selects one branch by strictly matching one expression against cases.",
    "Use break or return to prevent fall-through unless fall-through is intentional. If/else is often clearer for ranges.",
    "switch (value) { case match: ...; break; default: ... }",
    "switch (role) {\n  case \"admin\": return \"all\";\n  default: return \"limited\";\n}",
  ),
  "case": entry(
    "JavaScript keyword",
    "Defines one matching branch inside a switch statement.",
    "Case matching uses strict equality. Multiple case labels can share the same statements.",
    "case matchingValue:",
    "case \"open\":\n  return showOpenItems();",
  ),
  "default": entry(
    "JavaScript keyword",
    "Provides a fallback value or fallback branch depending on context.",
    "Inside switch, default handles unmatched cases. In parameters, = supplies a value only when the argument is undefined.",
    "function greet(name = \"Guest\") { ... }",
    "function greet(name = \"Guest\") {\n  return `Hello, ${name}!`;\n}",
  ),
  "while": entry(
    "JavaScript keyword",
    "Repeats a block while its condition stays truthy.",
    "Make sure something inside the loop changes the condition, or the loop may never finish.",
    "while (condition) { ... }",
    "let count = 0;\nwhile (count < 3) {\n  count += 1;\n}",
  ),
  "break": entry(
    "JavaScript keyword",
    "Stops the nearest loop or switch immediately.",
    "Execution continues after the stopped loop/switch.",
    "if (condition) break;",
    "for (const item of items) {\n  if (item.id === id) break;\n}",
  ),
  "continue": entry(
    "JavaScript keyword",
    "Skips the rest of the current loop iteration.",
    "The loop then moves to its next iteration. Use it to handle exclusions early.",
    "if (skipCondition) continue;",
    "for (const value of values) {\n  if (!Number.isFinite(value)) continue;\n  total += value;\n}",
  ),
  ".push": entry(
    "Array method · mutating",
    "Adds one or more items to the end of an array.",
    "It changes the original array and returns the new length—not the array.",
    "array.push(item1, item2, ...)",
    "const items = [1];\nitems.push(2); // items is [1, 2]",
  ),
  ".pop": entry(
    "Array method · mutating",
    "Removes and returns the last array item.",
    "It changes the original array and returns undefined when the array is empty.",
    "array.pop()",
    "const last = items.pop();",
  ),
  ".shift": entry(
    "Array method · mutating",
    "Removes and returns the first array item.",
    "It changes the original array and re-indexes the remaining items.",
    "array.shift()",
    "const first = queue.shift();",
  ),
  ".unshift": entry(
    "Array method · mutating",
    "Adds one or more items to the beginning of an array.",
    "It changes the original array and returns the new length.",
    "array.unshift(item)",
    "items.unshift(newFirstItem);",
  ),
  ".indexOf": entry(
    "Array/String method",
    "Returns the first matching index or -1 when no match exists.",
    "For simple existence checks, includes usually reads more clearly.",
    "value.indexOf(searchValue)",
    "[\"a\", \"b\"].indexOf(\"b\"); // 1",
  ),
  ".splice": entry(
    "Array method · mutating",
    "Removes, replaces, or inserts items inside an array.",
    "It changes the original array. Use slice or spread when you need a non-mutating result.",
    "array.splice(startIndex, deleteCount, ...newItems)",
    "items.splice(1, 1); // remove one item at index 1",
  ),
  ".join": entry(
    "Array method",
    "Combines array items into one string with a separator.",
    "It does not change the original array.",
    "array.join(separator)",
    "[\"red\", \"blue\"].join(\", \"); // \"red, blue\"",
  ),
  ".some": entry(
    "Array method",
    "Returns true when at least one item passes a condition.",
    "It can stop early after finding a match.",
    "array.some(item => condition)",
    "const hasAdmin = users.some(user => user.role === \"admin\");",
  ),
  ".every": entry(
    "Array method",
    "Returns true when every item passes a condition.",
    "For an empty array it returns true, which is called vacuous truth.",
    "array.every(item => condition)",
    "const allValid = fields.every(field => field.valid);",
  ),
  ".forEach": entry(
    "Array method",
    "Runs a callback once for each array item.",
    "It returns undefined and cannot be stopped early. Use map for transformation and for...of when you need break/continue.",
    "array.forEach(item => { ... })",
    "users.forEach(user => console.log(user.name));",
  ),
  ".sort": entry(
    "Array method · mutating",
    "Sorts an array in place and returns the same array.",
    "Without a comparator, values sort as strings. Copy first when the original order must be preserved.",
    "array.sort((a, b) => a - b)",
    "const sorted = [...numbers].sort((a, b) => a - b);",
  ),
  "Object.keys": entry(
    "Object method",
    "Returns an array of an object's own enumerable property names.",
    "Use Object.values for values or Object.entries for key/value pairs.",
    "Object.keys(object)",
    "Object.keys({ name: \"Ana\", age: 20 }); // [\"name\", \"age\"]",
  ),
  "Object.values": entry(
    "Object method",
    "Returns an array of an object's own enumerable property values.",
    "The result follows the object's property enumeration order.",
    "Object.values(object)",
    "Object.values({ a: 1, b: 2 }); // [1, 2]",
  ),
  "Object.entries": entry(
    "Object method",
    "Returns an array of [key, value] pairs.",
    "It works well with for...of and Object.fromEntries.",
    "Object.entries(object)",
    "for (const [key, value] of Object.entries(user)) {\n  console.log(key, value);\n}",
  ),
  "Object.hasOwn": entry(
    "Object method",
    "Checks whether an object directly owns a property.",
    "It is safer than calling object.hasOwnProperty directly on unknown objects.",
    "Object.hasOwn(object, propertyName)",
    "Object.hasOwn(user, \"email\");",
  ),
  "setTimeout": entry(
    "Web/Node timer",
    "Schedules a callback to run after at least the requested delay.",
    "The callback waits until the current call stack and earlier queued work allow it to run. Clear the timer when appropriate.",
    "const id = setTimeout(callback, milliseconds)",
    "const id = setTimeout(() => console.log(\"later\"), 1000);",
  ),
  "null": entry(
    "JavaScript value",
    "Represents an intentionally empty or absent value.",
    "Code usually assigns null deliberately, while undefined often means missing/unassigned. typeof null is the historical string \"object\".",
    "const selectedUser = null;",
    "return resultFound ? result : null;",
  ),
  "undefined": entry(
    "JavaScript value",
    "Represents a missing or unassigned value.",
    "Functions without a reached return statement produce undefined, and missing object properties also read as undefined.",
    "let value; // value is undefined",
    "const missing = user.unknownProperty; // undefined",
  ),
  "NaN": entry(
    "JavaScript number value",
    "Means Not-a-Number and represents an invalid numeric result.",
    "Its type is number, and NaN is not equal to itself. Check it with Number.isNaN.",
    "Number.isNaN(value)",
    "Number(\"hello\"); // NaN",
  ),
  "true": entry(
    "Boolean value",
    "Represents the positive boolean state.",
    "Conditions use truthiness, but functions that promise a boolean should return true/false explicitly or from a comparison.",
    "const isActive = true;",
    "return score >= 75;",
  ),
  "false": entry(
    "Boolean value",
    "Represents the negative boolean state and is itself falsy.",
    "Do not confuse the boolean false with the string \"false\", which is truthy because it is non-empty.",
    "const isComplete = false;",
    "Boolean(\"false\"); // true",
  ),
};
