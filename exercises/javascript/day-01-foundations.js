/**
 * DAY 1 — Foundations
 *
 * Rules:
 * - Replace each TODO with your own implementation.
 * - Do not change the function names or exports.
 * - Run: npm run test:day1
 * - Make it correct first. Refactor only after tests are green.
 */

/** Return `Hello, <trimmed name>!`. Use "Guest" for a blank/non-string name. */
export function greet(name) {
  // TODO
}

/** Return true only when value is a finite even number. */
export function isEven(value) {
  // TODO
}

/**
 * Return "negative", "zero", or "positive" for a finite number.
 * Return "invalid" for anything else, including NaN and Infinity.
 */
export function classifyNumber(value) {
  // TODO
}

/**
 * Calculate price * quantity after applying discountPercent.
 * Example: calculateTotal(100, 2, 10) === 180
 * Return 0 when quantity is 0.
 * Return null when any input is non-finite or negative, or discount > 100.
 */
export function calculateTotal(price, quantity, discountPercent = 0) {
  // TODO
}

/** Return the sum of only the finite even numbers in values. Non-array -> 0. */
export function sumEvenNumbers(values) {
  // TODO
}

/**
 * Return the largest finite number in values.
 * Ignore non-numbers, NaN, and Infinity. Return null if none exist.
 * Do not sort/mutate the input.
 */
export function findLargest(values) {
  // TODO
}

/**
 * Count words after trimming outer whitespace. One or more whitespace
 * characters separate words. Blank/non-string input -> 0.
 */
export function countWords(text) {
  // TODO
}

/**
 * Return a new user object with `active` toggled.
 * Return null for null, arrays, and non-object inputs.
 * Do not mutate the original object.
 */
export function toggleUserActive(user) {
  // TODO
}

