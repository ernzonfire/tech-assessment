/**
 * DAY 2 — Functions and Data
 * Run: npm run test:day2
 */

/** Return unique values in first-seen order. Use SameValueZero behavior. */
export function uniqueValues(values) {
  // TODO
}

/**
 * Return a new array sorted by numeric score descending, then name ascending.
 * Do not mutate the input.
 */
export function rankPlayers(players) {
  // TODO
}

/**
 * Group items into an object whose keys are each item's `status`.
 * Missing/blank status belongs to "unknown". Preserve item order.
 */
export function groupByStatus(items) {
  // TODO
}

/**
 * Summarize orders that have status "paid".
 * Each order has items with price and quantity.
 * Return { paidOrders, units, revenue }.
 */
export function summarizePaidOrders(orders) {
  // TODO
}

/**
 * Return a function that increments private state by `step` on each call.
 * makeCounter(10, 2) should produce 12, then 14.
 */
export function makeCounter(start = 0, step = 1) {
  // TODO
}

/**
 * Convert users into an object keyed by id. Later duplicate ids win.
 * Skip entries with null/undefined id. Do not mutate users.
 */
export function indexUsersById(users) {
  // TODO
}

/**
 * Split an array into arrays of at most `size` items.
 * Return [] for non-array input. Throw RangeError unless size is a positive integer.
 */
export function chunk(values, size) {
  // TODO
}

